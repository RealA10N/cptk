{% if system == 'Windows' -%}
{% set bin = slug(problem.name) ~ '.exe' -%}
{% else -%}
{% set bin = slug(problem.name) -%}
{% endif -%}
solution:
  bake: |
    g++ {{problem.name|slug}}.cpp -DLOCAL -O2 -o {{bin}}

  serve: |
    ./{{bin}}
